<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title | escape }}</title>
    <style>
        :root {
            color-scheme: light dark;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Dark mode (default and when prefers-color-scheme: dark) */
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
            background: linear-gradient(135deg, #0d2549 0%, #1a3a5c 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #ffffff;
        }

        .login-container {
            background: rgba(13, 37, 73, 0.98);
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4), 0 0 0 1px rgba(255, 255, 255, 0.15);
            padding: 40px;
            width: 100%;
            max-width: 400px;
            backdrop-filter: blur(10px);
        }

        .login-header {
            text-align: center;
            margin-bottom: 30px;
            color: #ffa500;
        }

        .login-header h1 {
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 10px;
            letter-spacing: -0.5px;
        }

        .login-header p {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.7);
            margin: 0;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group:last-of-type {
            margin-bottom: 30px;
        }

        label {
            display: block;
            font-size: 13px;
            font-weight: 500;
            margin-bottom: 8px;
            color: rgba(255, 255, 255, 0.9);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 12px 16px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 6px;
            background: rgba(255, 255, 255, 0.08);
            color: #ffffff;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        input[type="text"]:focus,
        input[type="password"]:focus {
            outline: none;
            border-color: rgba(255, 255, 255, 0.4);
            background: rgba(255, 255, 255, 0.12);
            box-shadow: 0 0 0 3px rgba(100, 200, 255, 0.1);
        }

        input[type="text"]::placeholder,
        input[type="password"]::placeholder {
            color: rgba(255, 255, 255, 0.5);
        }

        button[type="submit"] {
            width: 100%;
            padding: 12px 16px;
            background-color:  #00bccc;
            color: #ffffff;
            border: none;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 79, 163, 0.4);
        }

        button[type="submit"]:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 79, 163, 0.5);
            background-color: #8de1e8;
        }

        button[type="submit"]:active {
            transform: translateY(0);
            box-shadow: 0 2px 10px rgba(0, 79, 163, 0.4);
        }

        form {
            display: flex;
            flex-direction: column;
        }

        .advisory {
            margin-top: 20px;
            padding: 15px;
            background-color: rgba(100, 200, 255, 0.1);
            border-left: 4px solid #00bccc;
            border-radius: 6px;
            color: rgba(255, 255, 255, 0.95);
        }

        .advisory strong {
            color: #00bccc;
            text-transform: uppercase;
            font-size: 12px;
            letter-spacing: 0.5px;
            display: block;
            margin-bottom: 8px;
        }

        .advisory p {
            margin: 0;
            font-size: 13px;
            line-height: 1.5;
            color: rgba(255, 255, 255, 0.85);
        }

        .feedback {
            margin-top: 20px;
            padding: 15px;
            background-color: rgba(255, 100, 100, 0.15);
            border-left: 4px solid #ff6464;
            border-radius: 6px;
            color: #ff9999;
        }

        .feedback strong {
            color: #ff6464;
            text-transform: uppercase;
            font-size: 12px;
            letter-spacing: 0.5px;
            display: block;
            margin-bottom: 8px;
        }

        .feedback p {
            margin: 0;
            font-size: 13px;
            line-height: 1.5;
            color: #ffb3b3;
        }

        /* Light mode */
        @media (prefers-color-scheme: light) {
            body {
                background: linear-gradient(135deg, #f5f7fa 0%, #e8ecf1 100%);
                color: #1a1a1a;
            }

            .login-container {
                background: rgba(255, 255, 255, 0.95);
                box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1), 0 0 0 1px rgba(0, 0, 0, 0.08);
                backdrop-filter: blur(10px);
            }

            .login-header {
                color: #e67e22;
            }

            .login-header p {
                color: rgba(0, 0, 0, 0.6);
            }

            label {
                color: rgba(0, 0, 0, 0.8);
            }

            input[type="text"],
            input[type="password"] {
                border: 1px solid rgba(0, 0, 0, 0.15);
                background: rgba(0, 0, 0, 0.03);
                color: #1a1a1a;
            }

            input[type="text"]:focus,
            input[type="password"]:focus {
                border-color: rgba(0, 0, 0, 0.25);
                background: rgba(0, 0, 0, 0.05);
                box-shadow: 0 0 0 3px rgba(0, 123, 200, 0.1);
            }

            input[type="text"]::placeholder,
            input[type="password"]::placeholder {
                color: rgba(0, 0, 0, 0.4);
            }

            button[type="submit"] {
                background-color: #0099cc;
                box-shadow: 0 4px 15px rgba(0, 99, 200, 0.2);
            }

            button[type="submit"]:hover {
                background-color: #0088bb;
                box-shadow: 0 6px 20px rgba(0, 99, 200, 0.3);
            }

            button[type="submit"]:active {
                box-shadow: 0 2px 10px rgba(0, 99, 200, 0.2);
            }

            .advisory {
                background-color: rgba(0, 123, 200, 0.08);
                border-left-color: #0099cc;
                color: rgba(0, 0, 0, 0.85);
            }

            .advisory strong {
                color: #0099cc;
            }

            .advisory p {
                color: rgba(0, 0, 0, 0.75);
            }

            .feedback {
                background-color: rgba(220, 53, 69, 0.08);
                border-left-color: #dc3545;
                color: #c82333;
            }

            .feedback strong {
                color: #dc3545;
            }

            .feedback p {
                color: #a71930;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-header">
            <h1>{{ title | escape }}</h1>
        </div>
        <form method="post">
            <input type="hidden" name="csrf_token" value="{{ csrf_token | escape }}">
            <div class="form-group">
                <label for="user">Username</label>
                <input type="text" id="user" name="user" placeholder="Enter your username" required autofocus>
            </div>
            <div class="form-group">
                <label for="pw">Password</label>
                <input type="password" id="pw" name="pw" placeholder="Enter your password" required>
            </div>
            <button type="submit">Login</button>
        </form>
        {% if feedback %}
        <div class="feedback">
            {{ feedback | escape }}
        </div>
        {% endif %}
        {% if advisory %}
        <div class="advisory">
            {{ advisory | escape }}
        </div>
        {% endif %}
    </div>
</body>
</html>
